# Python deps for scripts/whep-watch-demux.py (stream.place WHEP → freeq).
# Installed by scripts/prep.sh / scripts/install-whep-deps.sh into the user env.
aiortc>=1.9.0
aiohttp>=3.9.0
av>=12.0.0
numpy>=1.26.0
