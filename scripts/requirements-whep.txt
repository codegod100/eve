# Python deps for scripts/whep-watch-demux.py (stream.place WHEP → freeq).
# Installed by scripts/install-whep-deps.sh:
#   preferred: system-manager whep-python, else nix build .#whep-python
#   fallback:  pip install --target ~/.local/share/eve/whep-python/lib
aiortc>=1.9.0
aiohttp>=3.9.0
av>=12.0.0
numpy>=1.26.0
