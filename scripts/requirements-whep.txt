# Deprecated for runtime: WHEP demux Python deps come from the flake.
#   nix build .#whep-python
#   bash scripts/install-whep-deps.sh
#
# Kept as a human-readable list of the packages in flake.nix whepPython:
#   aiortc, aiohttp, av, numpy
